---
import ButtonLink from "../components/ButtonLink.astro";
import PageHero from "../components/PageHero.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { aboutStats, aboutTimeline, founders, site } from "../content/site";
---

<BaseLayout
  title="About"
  description="Founder-led credibility for eCongruity strategy-to-implementation engagements."
>
  <PageHero
    eyebrow="About"
    title="Founder-led judgment for strategy and implementation."
    summary="eCongruity is guided by people who keep business judgment, Working Group alignment, and hands-on implementation connected through complex client work."
  >
    <div class="button-row">
      <ButtonLink href="/contact/">{site.contactCta}</ButtonLink>
    </div>
  </PageHero>

  <section class="section-band section-band--linen" aria-labelledby="mission-heading">
    <div class="site-container about-mission">
      <div>
        <p class="eyebrow">Our Mission</p>
        <h2 id="mission-heading">Simplifying the complex. Amplifying what matters.</h2>
        <p>
          eCongruity was founded on a simple belief: the right technology, applied with the right strategy, can transform how organizations operate and grow.
        </p>
        <p>
          We partner with leaders who are ready to think differently, move decisively, and build something that lasts. Our work sits at the intersection of strategy, technology, and human potential.
        </p>
      </div>
      <div class="mission-proof" aria-label="eCongruity mission proof">
        <blockquote>
          "Innovation isn't about technology. It's about people who refuse to accept that things can't be done better."
        </blockquote>
        <dl class="stat-grid">
          {
            aboutStats.map((stat) => (
              <div>
                <dt>{stat.value}</dt>
                <dd>{stat.label}</dd>
              </div>
            ))
          }
        </dl>
      </div>
    </div>
  </section>

  <section class="section-band section-band--green" aria-labelledby="story-heading">
    <div class="site-container about-story">
      <div>
        <p class="eyebrow">Our Story</p>
        <h2 id="story-heading">Where we come from.</h2>
        <p>
          eCongruity was born out of a frustration with how organizations approached change: reactively, in silos, and without a clear path forward.
        </p>
        <p>
          Founded in Livingston, Montana, we brought together strategists, technologists, and process designers with one goal: to help organizations find their clearest path forward, faster.
        </p>
        <p>
          Today we work with clients across the US, from enterprise organizations to nimble startups, bringing the same rigor and creativity to every engagement.
        </p>
      </div>
      <ol class="story-timeline">
        {
          aboutTimeline.map((item) => (
            <li>
              <p class="story-timeline__year">{item.year}</p>
              <h3>{item.title}</h3>
              <p>{item.summary}</p>
            </li>
          ))
        }
      </ol>
    </div>
  </section>

  <section class="section-band section-band--linen" aria-labelledby="founders-heading">
    <div class="site-container">
      <div class="section-header section-header--with-action">
        <div>
          <p class="eyebrow">Our Team</p>
          <h2 id="founders-heading">The people behind the work.</h2>
        </div>
        <p>
          Small by design, intentional by nature. We bring together strategists, technologists, and creative thinkers united by one belief: the best solutions grow at the intersection of people and purpose.
        </p>
      </div>
      <div class="team-grid">
        {
          founders.map((founder) => (
            <article class="founder-card">
              <div class="founder-card__header">
                {
                  founder.imageSrc ? (
                    <img
                      class="founder-card__image"
                      src={founder.imageSrc}
                      alt={founder.imageAlt ?? `${founder.name}, ${founder.role}`}
                      loading="lazy"
                      width="96"
                      height="96"
                    />
                  ) : (
                    <div class="founder-card__image" aria-hidden="true"></div>
                  )
                }
                <div>
                  <h3>{founder.name}</h3>
                  <p class="content-card__meta">{founder.role}</p>
                </div>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section-band section-band--stone" aria-labelledby="about-proof-heading">
    <div class="site-container closing-cta">
      <div>
        <p class="eyebrow">Client Stories</p>
        <h2 id="about-proof-heading">See how the positioning is supported by proof.</h2>
        <p>
          Client stories are the preferred proof format for eCongruity: short snapshots that show leaders moving from unclear complexity toward implemented, maintainable change.
        </p>
      </div>
      <div class="button-row button-row--compact">
        <ButtonLink href="/#proof-heading" variant="secondary">View Home Page Proof</ButtonLink>
        <ButtonLink href="/contact/">{site.contactCta}</ButtonLink>
      </div>
    </div>
  </section>
</BaseLayout>
